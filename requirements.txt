# Pinned to the versions used to produce the results in /results.
# Newer versions are likely to work; see environment.yml for an exact conda spec.
moabb>=1.0.0
mne>=1.6.0
pyriemann>=0.6
scikit-learn>=1.3
scipy>=1.11
statsmodels>=0.14
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
